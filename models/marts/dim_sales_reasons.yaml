version: 2

models:
  - name: dim_sales_reason
    description: "Dimension table containing sales reasons, including their type and association with sales orders."
    columns:
    - name: FK_SALES_ORDER_ID
      description: "Foreign key referencing the sales order associated with the reason."


    - name: SALES_REASON
      description: "The informed sales reason"


    - name: SALES_REASON_TYPE
      description: "The informed sales reason type"

